# YAML frontmatter 规范

## 标准格式

```yaml
---
search: false
---
```

## 规则

- frontmatter 放在 `# 标题` **之前**
- 正确顺序：`---` → `<div align="center">` → `# 🛠️ 标题`
- README 顶行必须是 `---`，**不是** `# 标题`
- 禁止将 SKILL.md 的 frontmatter 复制到 README
